<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.battleships_0">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WinLose"
            android:theme="@style/AppTheme.WinLoseTheme"
            ></activity>
        <activity android:name=".Play">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PlayerField" />
        <activity android:name=".Ask" />
        <activity android:name=".AdminEditor" />
        <activity android:name=".AdminCategories" />
        <activity android:name=".AdminLogin" />
        <activity android:name=".Fields" />
        <activity android:name=".Questions" />
        <activity android:name=".Categories"></activity>
    </application>

</manifest>